[package]
name = "velocity-browser"
version = "0.1.0"
edition = "2021"

[dependencies]
velocity-ide = { path = "../velocity-ide" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
tempfile = "3.8"

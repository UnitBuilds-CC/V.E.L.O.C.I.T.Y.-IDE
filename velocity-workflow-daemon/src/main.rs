//! Velocity Workflow Daemon — crash-isolated workflow execution engine.
//!
//! This is a standalone daemon that uses the workflow engine from `shared/`
//! to execute workflows in an isolated process. It communicates with the
//! main IDE via IPC.
//!
//! **Status:** Skeleton — not yet implemented.

fn main() {
    eprintln!("velocity-workflowd: not yet implemented");
    std::process::exit(1);
}

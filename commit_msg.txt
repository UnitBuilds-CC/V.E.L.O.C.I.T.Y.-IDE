Block 201: x86_emitter.rs — 24 new tests (115 total)

Coverage areas:
- gemv_native: output size verification, correct dimensions with NdaMatrix::new_quad
- is_pure_scalar: SiLU not eligible (only Negate/Abs/ReduceSum), nested Add of Loads,
  if with impure then/else body
- native_compile_info: Norm node interpreter-only, multiple variables count,
  JSON all 11 fields present, total=eligible+interpreter invariant,
  while sets both loop flags, no-ops flags for pure nodes,
  empty has no flags, if without else still sets conditionals
- count_nodes: empty while body, nested scopes, all CmpOp variants
- emitter_diagnostic: short buffer (2 bytes), 3 bytes no ret, multiple issues count,
  validation issues >= 2 for empty buffer
- emitter operations: mov_eax_imm32 length, multiple mov_eax last wins,
  multiple emits accumulate correctly

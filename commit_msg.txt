Block 199: nda_parser.rs — 42 new tests (158 total)

Coverage areas:
- hash_name: distribution (100 unique), similar prefixes, case sensitivity
- build_matrix_node: bitmap byte count formulas, odd cols, 1x1 exact bytes
- build_norm_node: bitmap bytes for 8/9, large clamped, weight/bias content
- resolve_calls: nested if/else, unresolved name stays original, no call_names entry
- compile vs compile_with_report equivalence
- ParseReport: unresolved calls, JSON hash format (16 hex chars), sorted names
- parse_type: vec/matrix/norm/int params, int/vec return types
- parse expressions: vec bracket/paren, norm paren, abs/reduce_sum/negate builtins
- Nested control flow: nested loops, else-if chains, while inside loop
- Error paths: unexpected token in let, missing colon, missing rparen, missing comma
- Misc: empty function body, multiple calls same fn, call target hash, optional semicolons,
  negative int literal, double negate, add chain left-associativity

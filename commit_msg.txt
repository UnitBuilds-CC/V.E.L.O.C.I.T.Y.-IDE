Block 187: credential_guard.rs — 25 new tests (111 total)

New tests cover:
- JSON key counts for CredentialAuditSummary (6), CredentialScopeReport (6), CredentialBoundaryAudit (4)
- JSON roundtrip via Value for all 3 diagnostic structs
- JSON type verification for CredentialAuditSummary and CredentialBoundaryAudit
- Clone independence for CredentialAuditSummary and CredentialScopeReport
- Debug format for CredentialAuditSummary
- Masked edge cases: 13-char boundary, empty string
- SecretString zeroize then verify all bytes zero with length preserved
- CredentialScope scrub preserves env_vars_scrubbed list
- Audit report JSON contains no secret values
- Env scrubbing: XAI_API_KEY, CR_PAT, AWS_SESSION_TOKEN, AWS_SECURITY_TOKEN
- Audit env exposure dedup for SSH_AUTH_SOCK
- Sensitive config paths: .npmrc, .pypirc, exact count (10)
- Compact JSON (no whitespace)
- Warning message structure with all 3 categories
- SENSITIVE_ENV_VARS exact count (28)

Full suite: 4596 lib tests, 0 failures

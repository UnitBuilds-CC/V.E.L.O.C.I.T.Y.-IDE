main.rs — 24 new tests (162 total)

Block 207: resolve_config field coverage (vocab_size, n_heads, max_seq_len
for both qwen and bitnet), resolve_model_dir additional paths (None errors,
existing dir clone), resolve_tokenizer auto-discover (None falls through,
finds tokenizer.json in model dir), CloudflareAccount struct fields,
CliDiagnostics with velocity_config Some/None, GenerationReport extreme
values (elapsed_ms zero, u64::MAX, empty mode, large site_map counts),
Message edge cases (JSON array content, JSON object in content, null bytes)

pipeline_bridge.rs — 26 new tests (163 total)

Block 203: pipeline_bridge.rs — EngineOutput variant field access (Text/Nda),
validate() logic multiple warnings + divisibility, boundary values (u64::MAX elapsed_us/root_hash),
MHA/MQA/GQA attention patterns, site_map zero-total guard, per_second extreme values,
scope_similarity boundary (0.0/1.0), pretty JSON, clone preserves all fields,
tokenizer_merge_count zero, vocab_size boundary

Block 198: pipeline_bridge.rs — 24 new tests (137 total)

JSON value types verification for all report fields, nested struct
serialization (EngineStatusSnapshot inside EngineReport, NdaRunDiagnostics
inside EngineReport), unicode and special char roundtrips, empty string
edge cases, nested clone independence (status snapshot and NDA diagnostics
inside report), fractional per_second calculations, optional field
combinations (all_some, mixed, sandbox_only), path consistency checks
(text vs nda mutual exclusivity), boundary values (usize::MAX for
prompt_tokens and output_count), site_map_stats string content, model_dir
with special chars and path separators, hit rate formula edge cases,
stored per_second matches formula, all derived fields consistent for
Qwen-0.5B style config.

Block 211: transformer.rs — 26 new tests (167 total), committed

New tests cover:
- attention_head (bitwise popcount Q·K): empty KV, single block, output length, offset head
- pack_vector helper: delegation to impl, empty slice
- TransformerInfo computed fields: head_dim, GQA ratio, all_gpu_false, multiple validation issues
- Fp32GenerationReport edge cases: zero tokens, elapsed boundary, both stopped+truncated, forward metrics count
- Fp32ForwardMetrics: gpu_active true, boundary values (usize::MAX, u64::MAX)
- NdaKvBlock: clone independence, large bitmaps (128-dim)
- KvLayer: single element push, bitmap bytes formula
- sample_token: two-logit tie, very small top_p
- apply_rope_head: pos=0 full identity verification
- silu: boundary at 0.5
- rms_norm: large values
- lm_head: 4-vocab × 2-hidden parallel correctness

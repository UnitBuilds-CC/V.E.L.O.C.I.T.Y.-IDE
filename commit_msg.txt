Phase N: IDE client expansion — 7 new methods and response types

- echo() — GET /v1/echo connectivity test
- estimate_assignment() — POST /v1/assignments/estimate cost preview
- submit_batch() — POST /v1/assignments/batch multi-task submission
- get_billing() — GET /v1/usage/billing period summary
- get_analytics() — GET /v1/analytics usage analytics
- get_provider_health() — GET /health/providers circuit breaker status
- 7 new response types: EchoResponse, EstimateResponse, BatchResponse,
  BatchResultEntry, BillingResponse, AnalyticsResponse, ProviderHealthResponse,
  ProviderStatus
- 4 new unit tests for response deserialization
- 104 velocity_client tests pass

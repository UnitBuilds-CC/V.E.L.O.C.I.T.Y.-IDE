Block 196: main.rs — 25 new tests (140 total)

JSON key counts (CliEnvironment=7, CliDiagnostics=3), validate_generate_args
(multiple issues, temperature/top_p boundaries, both prompt+prompt_file),
validate_chat_args (temperature 0, top_p boundaries, multiple issues),
validate_seed_args (empty source, invalid hex, valid hex with 0x prefix),
Message edge cases (empty role/content, unicode roundtrip), CliEnvironment
serialization/clone independence, GenerationReport display all fields,
LoginArgs/ProvidersArgs defaults and optional fields.
